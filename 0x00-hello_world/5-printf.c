#include <stdio.h>
/**
 * main - a c program that print a line with printf function
 * Return: Always 0 (success)
 */
int main(avoid)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
